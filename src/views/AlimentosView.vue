<script setup>
import { ref } from 'vue';
import { listaProdutos } from '@/data/produtos';
const produtos = ref(listaProdutos);
import ProdutoCard from '@/components/ProdutoCard.vue';
produtos.value = produtos.value.filter(p => p.categoria === 'Alimentos')
</script>
<template>
<div class="container">

  <h1>Alimentos</h1>
  <div class="produtos">
<ProdutoCard v-for="produto in produtos" :key="produto.id" :nome="produto.nome"
 :preco="produto.preco" :categoria="produto.categoria" :imagem="produto.imagem"></ProdutoCard>
  </div>
</div>
</template>

<style scoped>
</style>
